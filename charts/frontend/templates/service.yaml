apiVersion: v1
kind: Service
metadata:
  name: um-frontend-service
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: um-frontend
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000